[flake8]
# Exclude these directories from being checked at all
exclude =
    */migrations/*,
    __init__.py,
    djangoenv
# Ignore the "line too long" error because it's tedious to fix manually
ignore = E501
